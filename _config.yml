# Setup
title:               Heat Map Visualization
tagline:             Visualization using D3.js

# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          css
  style:            :compressed
